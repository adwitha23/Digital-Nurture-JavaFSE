Recursion is a technique in which a method calls itself to solve a smaller version of the same problem. It simplifies the problem by breaking it into smaller and easier steps.

The recursive algorithm has a time complexity of O(n) because it makes one recursive call.

The recursive solution can be optimized by using iteration. This avoids unnecessary recursive calls and thus improves performance.