JPA is a set of rules (specification) for how Java should handle database mapping, while Hibernate is a tool that actually implements those rules and does the ORM work.  
Spring Data JPA sits on top of JPA/Hibernate and gives ready-made methods to reduce boilerplate code for common database operations.
